
***

* Full name: `\CoralMedia\IR\LinearAlgebra`
* This class is marked as **final** and can't be subclassed
* This class is a **Final class**

## Methods

### dot

Computes the dot product between two vectors.

```php
public static dot(array $x, array $y): float
```

* This method is **static**.
**Parameters:**

| Parameter | Type      | Description                                   |
|-----------|-----------|-----------------------------------------------|
| `$x`      | **array** | First vector.                                 |
| `$y`      | **array** | Second vector. Must have same length as `$x`. |

***

### normL2

Computes the Euclidean norm (L2) of a vector.

```php
public static normL2(array $x): float
```

* This method is **static**.
**Parameters:**

| Parameter | Type      | Description   |
|-----------|-----------|---------------|
| `$x`      | **array** | Input vector. |

***
